const Job = require('../models/job'),
      Company = require('../models/companyAcc.js');

/*eslint-disable */
module.exports.create_company_acc = (req, res, next) => { /*eslint-enable */
  var company = new Company(() => {
    
  });
}
